{
  "name": "renkai-player-agent",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "description": "Standalone Renkai player skill and zero-dependency CLI for Hermes and OpenClaw agents.",
  "scripts": {
    "check": "node --check scripts/renkai.mjs && npm test",
    "test": "node --test scripts/renkai.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
